# Vercel env vars for /api/wycena.
# Set these in Vercel → Project → Settings → Environment Variables.
# Never commit real values.

RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx

# Owner inbox — placeholder until Meeting #2 confirms Tomasz's business email
WYCENA_TO_EMAIL=baloskar84@gmail.com

# Verified Resend sender domain (carsdetailingradom.pl when DNS lands; until then Bal Agency's verified sender)
# NEVER use onboarding@resend.dev — recipient-restricted, will silently fail for non-account-owner inboxes.
WYCENA_FROM_EMAIL=wycena@balagency.pl

# Optional — Reply-To when customer didn't leave their email
WYCENA_REPLY_TO=baloskar84@gmail.com
